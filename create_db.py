"""
this creates a database on mysql
"""
# import mysql.connector

# db = mysql.connector.connect(
#     host="", put host you're connecting to e.g localhost
#     user="", name of user e.g root
#     passwd="" password for the user
# )

# db_cursor = db.cursor()

# db_cursor.execute("CREATE DATABASE IF NOT EXISTS bakers_kiss")

# db_cursor.execute("SHOW DATABASES")

# for d in db_cursor:
#     print(d)